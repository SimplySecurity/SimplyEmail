jQuery(function($){

	$( "#ui-tabs" ).tabs();
	
	var tab = $('#ui-tabs ul.ui-tabs-nav li'),
		tabParent = $('#ui-tabs ul.ui-tabs-nav li').parent(),
		numTabs = tab.length;
		
	tab.width(100/numTabs+'%');
	
	tab.height(tabParent.height());

	$(window).resize(function() {

	}).resize();

});